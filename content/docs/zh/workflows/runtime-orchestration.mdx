---
title: 运行时编排
description: 让 Agent 在运行期间重塑 workflow 的执行形状。
---

运行时编排让 workflow 不只是静态图。

Agent 可以在运行中 handoff 状态、更新计划、改变执行形状，并派生并行子任务。

## 常见操作

- 结构化 handoff。
- 更新计划。
- 创建并行子任务。
- 调整路由。
- 人工介入。

## 设计原则

运行时变化必须可观察。如果 workflow 改变形状，用户应该能看到原因。
