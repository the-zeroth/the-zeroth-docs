---
title: 行动链（Action Sequence）
description: 用行动链把复杂目标拆成更可执行的阶段路径。
---

作为 ReAct 思想的延申，The One 中设计了一套名为行动链的内置 Tool：

ReAct 的思想是让 Agent 自发地朝着一个任务不断行动并逐渐接近目标。

可是如果你了解强化学习相关的知识，你就知道对于复杂的任务，对最终目标的直接追求往往会让模型在一条死路上无限撞墙；The One 的解决方案便是提供一套 Tool——让 Agent 自发地将一个大的任务拆解成一条包含了若干小的行动目标的实现路径，称之为行动链。
